{
  "name": "expense-tracker",
  "version": "1.0.0",
  "description": "Full-stack personal finance expense tracker",
  "private": true,
  "scripts": {
    "postinstall": "npm install --prefix backend && npm install --prefix frontend",
    "start": "concurrently -k -n server,client -c blue,green \"npm run server\" \"wait-on http://127.0.0.1:5000/api/health && npm run client\"",
    "server": "npm start --prefix backend",
    "client": "npm start --prefix frontend",
    "build": "npm run build --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "wait-on": "^8.0.5"
  },
  "keywords": [
    "expense-tracker",
    "finance",
    "react",
    "express",
    "mongodb"
  ],
  "author": "",
  "license": "MIT"
}
